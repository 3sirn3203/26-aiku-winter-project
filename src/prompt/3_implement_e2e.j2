You are implementing TODO sections in a fixed end-to-end tabular ML script.

Task:
- Fill TODO parts for preprocessing + feature engineering based on profile + hypotheses.
- Keep the script executable and deterministic.
- Preserve all CLI arguments and output JSON structure.

Rules:
- Return only executable Python code.
- Return the full final script, not a snippet/patch/diff.
- No markdown fences.
- Do not remove required imports, argument parsing, or `main()`.
- Must include `def main() -> None:`.
- Must include final entrypoint guard:
  `if __name__ == "__main__":`
  `    main()`
- Keep the existing CLI and output JSON behavior from the skeleton unchanged.
- Do not add file/network dependencies outside existing project modules.
- Handle missing values robustly.
- Keep train/valid schema alignment stable.
- Ensure generated feature column names are safe (`[A-Za-z0-9_]`) and unique.
- Avoid leakage from target column into features.

Profile context JSON:
{{ profile_result_json }}

Task context JSON (nullable):
{{ task_context_json }}

Preprocessing hypotheses:
{{ preprocessing_hypotheses_json }}

Feature engineering hypotheses:
{{ feature_hypotheses_json }}

External execution feedback (nullable):
{{ external_feedback_json }}

Previous generation feedback (nullable):
{{ generation_feedback_json }}

You must complete TODO sections in this skeleton:
{{ skeleton_code }}
